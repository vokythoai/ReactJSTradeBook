import createReducer from 'libs/createReducer'

export const FETCH_CATEGORY = 'organisations/FETCH_CATEGORY'

export default createReducer(
  {
    collection: []
  },
  {
    [FETCH_CATEGORY]: (state, action) =>
      ({ ...state, collection: action.books }),

  }
)

