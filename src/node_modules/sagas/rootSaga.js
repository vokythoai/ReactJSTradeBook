import {takeEvery, takeLatest} from 'redux-saga/effects'
import fetchCategoryBookSaga from './bookSaga'

import {
  FETCH_CATEGORY
} from 'store/categories'

export default function * mainSaga () {
   yield [
    takeEvery(FETCH_CATEGORY, fetchCategoryBookSaga)
   ]
}
